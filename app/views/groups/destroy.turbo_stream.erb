<% if @has_opened_list %>
    <%= turbo_stream.close_list_panel %>
<% end %>

<% if @has_opened_task %>
    <%= turbo_stream.close_task_panel %>
<% end %>

<% if (@groups_count - 1) == 0 %>
  <%= turbo_stream.update "newGroup", "<p>Create something here..."%>
<% end %>

<%= turbo_stream.remove @group %>